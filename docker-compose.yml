services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "8081:80"
    restart: always

  backend:
    build:
      context: ./backend
    ports:
      - "8082:80"
    restart: always
